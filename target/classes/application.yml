spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
      dialect: org.hibernate.dialect.MySQL5Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/student?useSSL=false
    username: root
    password: Root$1234
server:
   port: 9091       